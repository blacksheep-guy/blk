# Copy to .env.local for `vercel dev`
SESSION_SECRET=change-me-to-a-long-random-string

# Optional — used inside authenticate() when you wire real auth
# AUTH_URL=https://your-auth-service.example.com/login
