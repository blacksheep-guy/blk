{
  "name": "bektel-webmail",
  "version": "1.0.0",
  "private": true,
  "description": "bektel.com webmail login — Vercel Edge middleware backend",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Static files are in /public'",
    "start": "npx serve public"
  },
  "dependencies": {
    "@vercel/edge": "^1.2.1"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vercel": "^39.1.0"
  }
}
